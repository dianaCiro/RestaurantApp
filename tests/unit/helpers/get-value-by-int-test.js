
import { getValueByInt } from 'busqueda-restaurante/helpers/get-value-by-int';
import { module, test } from 'qunit';

module('Unit | Helper | get value by int');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getValueByInt([42]);
  assert.ok(result);
});

